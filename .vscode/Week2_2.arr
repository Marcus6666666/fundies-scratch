use context starter2024
semiaxes_a = input("Please enter the first value")
semiaxes_b = input('Please enter the second value')
#asking the user to enter the vlaues of ellipse#
import math
#importing the library so I can use the pi value#
a = float(semiaxes_a)
b = float(semiaxes_b)
#changing the value type to float for calculation#
Area = math.pi * a * b
print(Area)
